alembic==1.19.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
bcrypt==4.3.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
cryptography==50.0.0
ecdsa==0.19.2
fastapi==0.141.1
greenlet==3.5.4
h11==0.16.0
idna==3.18
Mako==1.4.1
MarkupSafe==3.0.3
passlib==1.7.4
psycopg2-binary==2.9.12
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
python-jose==3.5.0
requests==2.34.2
rsa==4.9.1
six==1.17.0
SQLAlchemy==2.0.51
starlette==1.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.1
httpx[http2]==0.28.1
# IANA timezone database. Windows ships no system tzdata, so ZoneInfo
# ("Asia/Kolkata") needs this package to resolve.
tzdata==2026.1

# Screenshot upload (app/api/time_entry_screenshot.py). FastAPI parses
# multipart/form-data through this; without it the upload endpoint raises at
# import time rather than at request time.
python-multipart==0.0.20

# Screenshot storage. The service account credentials live only on this
# backend -- the desktop client never receives them -- and these are what talk
# to Drive (app/services/google_drive_service.py).
google-api-python-client==2.187.0
google-auth==2.42.1

# Reads the real geometry of an uploaded screenshot, so a client cannot claim
# 1000x1000 and store something else. Optional at runtime: without it the
# dimension check is skipped and the format check still applies.
Pillow==12.1.0
