Phase 0 — Repository scaffolding (current)
Phase 1 — Identity: users table, refresh_tokens table, login-exchange endpoint, JWT middleware
Phase 2 — Core CRUD: projects, tasks, project members, task assignees
Phase 3 — Time tracking: automatic + manual entries, approval workflow
Phase 4 — Activity/app/URL tracking ingestion + desktop capture modules
Phase 5 — Screenshot capture + Google Drive pipeline
Phase 6 — Reports
Phase 7 — Offline sync hardening (desktop)
Phase 8 — Notifications + future extensions

Only work on the phase marked "current" unless a task explicitly says otherwise.