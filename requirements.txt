# --- Shared / Backend ---
alembic==1.19.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
bcrypt==4.3.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
cryptography==50.0.0
ecdsa==0.19.2
fastapi==0.141.1
greenlet==3.5.4
h11==0.16.0
idna==3.18
Mako==1.4.1
MarkupSafe==3.0.3
passlib==1.7.4
psycopg2-binary==2.9.12
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
python-jose==3.5.0
requests==2.34.2
rsa==4.9.1
six==1.17.0
SQLAlchemy==2.0.51
starlette==1.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.1
httpx==0.28.1

# --- Screenshot storage (backend/app/services/google_drive_service.py) ---
# Vercel installs THIS file for api/index.py, so anything the deployed backend
# imports has to be listed here too, not only in backend/requirements.txt.
google-api-python-client==2.187.0
google-auth==2.42.1

# FastAPI parses multipart/form-data through this; the screenshot upload
# endpoint raises at import time without it.
python-multipart==0.0.20

# Reads the real geometry of an uploaded screenshot, so a client cannot claim
# 1000x1000 and store something else.
Pillow==12.1.0

# IANA timezone database, for ZoneInfo("Asia/Kolkata").
tzdata==2026.1

# --- Desktop ---
# Note: Do not include PySide6 here as it breaks Vercel serverless deployments due to size limits.
# Install it separately for desktop development.